### Control Flow:

If-else: branching logic

Loops: repeating logic

Match: powerful pattern matching

```rust
fn is_even(num: i32) -> bool {
    if num % 2 == 0 {
        true
    } else {
        false
    }
}

```
