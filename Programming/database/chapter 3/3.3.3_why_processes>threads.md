### Why PostgreSQL Uses PROCESSES > Threads? 🛡️

```text
| PROCESSES ✅             | THREADS ❌ (MySQL)          |
|--------------------------|-----------------------------|
| PID 12347 crashes? → Others LIVE ✅ | Thread #47 crashes? → WHOLE DB DEAD 💀 |
| Private 2MB memory/PID   | Shared memory = BUG FIGHTS  |
| `kill 12347` = INSTANT FIX | Thread dump = DEBUG HELL!   |
| Fork() = FREE copy (CoW) | Locks EVERYWHERE = SLOW!    |
```

### Real Crash Scenarios

```text
1000 users clicking "Login" 👥

PROCESSES (PostgreSQL):
PID 12347 has BUG → CRASH 😵
→ 999 PIDs keep serving users! ✅
→ `kill 12347` → Postmaster forks NEW PID ⚡

THREADS (MySQL):
Thread #47 has BUG → CRASH 😵
→ 1000 users see "DB DOWN" 💥
→ Restart WHOLE server (5min) 😭
```

### Memory Safety = LIFE OR DEATH

```text
PROCESSES:
┌─ PID 12347 ──┐ ┌─ PID 12456 ──┐
│ 2MB private   │ │ 2MB private   │
│ CRASH = SAFE! │ │ Keeps working!│
└───────────────┘ └───────────────┘

THREADS:
┌─────────────────────────────────────┐
│ ALL threads share SAME memory!      │
│ Thread #47 segfault → ALL DEAD! 💀  │
└─────────────────────────────────────┘
```

### Debugging = NIGHTMARE vs SIMPLE

```text
PROCESSES:                    THREADS:
`ps aux | grep postgres`      `gdb -p $(pgrep mysqld)`
`kill 12347`                  Restart WHOLE server
`strace -p 12347`             Cross-thread debugging HELL
NEW PID forks instantly! 🔄   5min downtime 😭
```
